学习笔记
1、位运算：
   （1）异或：相同为0，不同为1.
	x ^ 0 =x
	x ^ 1s = ~x;
	x ^ (~x) = 1s
	x ^ x = 0
	c = a ^ b => a ^ c = b, b ^ c = a
	a ^ b ^ c = a ^ (b ^ c) = (a ^ b ) ^ c
    （2）指定位置的位运算
	将x最右边的n位清零：x & (~0 << n)
	获取x 的第n位值（0 或者 1) : (x >> n) & 1
	获取x的第n位的幂值： x & (1 << n)
	仅将第n位置为1： x | (1 << n)
	仅将第n位置为0： x & (~(1 << n))
	将x最高位至第n位（含）清零： x & ((1 << n) - 1)
    （3）运算要点：
	判断奇偶： x & 1 == 1 or 0;
	x >> 1 -> x / 2
	x = x & (x - 1) 清零最低位的1
	x & -x 得到最低位的1
	x & ~x = 0
2、布隆过滤器
    （1）优点：空间效率与查询时间都远远超过一般的算法
    （2）缺点：有一定的误识别率和删除困难
3、LRU Cache
    （1）两个要素：大小、替换策略
    （2）hash table  +  double linklist
    （3）查询o(1)、修改、更新o(1)
4、排序算法
    （1）分类：比较类排序、非比较类排序
    （2）o(nlogn) 的排序：堆排序、快速排序、归并排序
